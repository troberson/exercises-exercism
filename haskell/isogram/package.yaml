name: isogram
version: 1.4.0.4

dependencies:
  - base

library:
  exposed-modules: Isogram
  source-dirs: src
  dependencies:
    - containers

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - isogram
      - hspec
